---
status: done
needs-human: false
---

# Remove stale file references in .gitignore and CLAUDE.md

## PRD

[2026-05-31-remove-vestigial-customization](../prd.md)

## What to build

Stop two config/doc files from pointing at things this fork never produces.

`.gitignore`: remove the three entries that reserve slots for files/dirs that do
not and cannot exist here —

- `hammerspoon/windows-bindings.lua` (the override file being designed out).
- `hammerspoon/hyper-apps.lua` (hyper mode was never ported into this fork).
- `karabiner/automatic_backups` (no `karabiner/` directory; the
  karabiner-elements cask is commented out in the `Brewfile`).

`CLAUDE.md`: update the Window-layout-mode subsystem line that currently reads
`windows.lua` (+ `windows-bindings*.lua`) so it no longer references the deleted
bindings file pair, keeping the router accurate.

No `.gitignore` entry or doc line beyond these is touched; the `Brewfile`
karabiner comment stays as-is (out of scope).

## User stories covered

9, 10

## Acceptance criteria

- [ ] The three named entries are removed from `.gitignore` and no other entries
      are changed.
- [ ] The `CLAUDE.md` Window-layout-mode subsystem line no longer references
      `windows-bindings*.lua`.
- [ ] `make lint` and `make fmt-check` stay green.

## Blocked by

None - can start immediately
