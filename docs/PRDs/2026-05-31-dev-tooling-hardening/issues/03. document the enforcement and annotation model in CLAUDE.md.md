---
status: todo
needs-human: false
---

# Document the enforcement and annotation model in CLAUDE.md

## PRD

[2026-05-31-dev-tooling-hardening](../prd.md)

## What to build

Extend CLAUDE.md's "Editor and tooling" section so the hardened setup is discoverable and a future agent does not "fix" the deliberate choices. Document what actually landed:

- The **lefthook pre-commit enforcement model** and the deliberate **no-CI stance**, pointing at `docs/adr/0002-enforce-checks-via-lefthook-not-ci.md`.
- The **shellcheck addition** (over `script/*`, folded into `make lint` and the hook).
- The **Engine annotation rationale**: engine-only scope, moderate diagnostics, and *why* strict is avoided (the `setmetatable`-OO false-positive flood). This rationale lives in CLAUDE.md rather than an ADR because annotations are trivially reversible.

The `CONTEXT.md` glossary and the two ADRs (`0001-keep-space-fn-adapter-thin`, `0002-enforce-checks-via-lefthook-not-ci`) were written during the design session — reference them, do not recreate them.

## User stories covered

- 27: CLAUDE.md documents the enforcement model, shellcheck addition, and Engine annotation rationale.
- 28: the two lock-in decisions are recorded as ADRs (already written; this issue references them).

## Acceptance criteria

- [ ] CLAUDE.md's "Editor and tooling" section documents the lefthook pre-commit enforcement model and the no-CI stance, pointing at ADR 0002.
- [ ] The shellcheck addition is documented (scope over `script/*`, folded into `make lint` and the hook).
- [ ] The Engine annotation rationale is documented: engine-only scope, moderate diagnostics, and why strict is avoided.
- [ ] The section references the existing ADRs and `CONTEXT.md` rather than recreating their content.

## Blocked by

- [02. enforce checks with a lefthook pre-commit hook.md](<./02. enforce checks with a lefthook pre-commit hook.md>)
