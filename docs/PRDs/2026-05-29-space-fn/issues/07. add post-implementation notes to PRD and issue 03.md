---
status: done
needs-human: false
---

# Add post-implementation notes to PRD and issue 03

## PRD

[2026-05-29-space-fn](../prd.md)

## What to build

Append clearly-marked errata sections to the PRD and issue 03 so future readers see both the original intent at the time of writing AND the as-shipped reality, without silent edits to the body of either document.

**For the PRD** (`docs/PRDs/2026-05-29-space-fn/prd.md`): append a new `## Post-implementation notes` section at the very end of the file (after "Further Notes"). Include two dated entries (use the actual date at fix time):

1. **Re-entrancy guard mechanism.** The PRD's "Re-entrancy guard" section describes a Lua-level `state.emittingSelf` flag. The shipped implementation in `hammerspoon/space-fn.lua` instead tags synthetic events with `eventSourceUserData = SYNTHETIC_MARKER` and the tap callback short-circuits on that property. Same goal, no flag-vs-async-post race window.
2. **Buffer dedup in `pending`.** Issue 06's fix added an idempotency rule to the engine's `pending` state: duplicate `key-down` events for the same key produce one buffer entry. The PRD's "State machine" section's `pending` row describes the original behavior. Erratum: the rule is now "buffered key-down is idempotent on key, to absorb macOS autorepeat without duplicating emissions on commit."

**For issue 03** (`docs/PRDs/2026-05-29-space-fn/issues/03. wire space-fn adapter and delete scratchpads.md`): append a short errata paragraph at the end of the file noting the re-entrancy mechanism change (same content as #1 above, shorter).

Voice for both: plain language, no em dashes.

## User stories covered

- Cross-cutting fix from final review

## Acceptance criteria

- [ ] PRD has a `## Post-implementation notes` section at the end with two dated entries (re-entrancy mechanism, buffer dedup erratum).
- [ ] Issue 03 has a short errata paragraph at the end noting the re-entrancy mechanism.
- [ ] Original PRD and issue 03 body content is NOT silently edited — corrections live only in clearly-marked errata.
- [ ] No em dashes in the new content.

## Blocked by

- [06. fix stuck-state and autorepeat bugs from final review.md](<./06. fix stuck-state and autorepeat bugs from final review.md>)
