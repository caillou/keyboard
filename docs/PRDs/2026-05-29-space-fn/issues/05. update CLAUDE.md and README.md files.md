---
status: done
needs-human: false
---

# Update CLAUDE.md and README.md files

## PRD

[2026-05-29-space-fn](../prd.md)

## What to build

Bring both top-level docs in line with the post-space-fn reality so a future agent (or future-me) doesn't reflexively avoid the new toolchain or get steered toward the deleted scratchpads.

**CLAUDE.md.** Remove the "no package manager beyond Homebrew" bullet from the "Deliberate non-choices" section — that stance was retired when LuaRocks came in for tests. Add a short section documenting the testing setup: busted as the framework, LuaRocks as the package manager, project-local `lua_modules/`, `make test` / `make test-watch` as the entry points, specs under `spec/`. Update the architecture overview to describe `space-fn-engine.lua` (pure state machine) and `space-fn.lua` (eventtap adapter) in place of the three deleted `super*.lua` scratchpads, and remove the warning that treated those files as scratchpads.

**README.md.** Currently essentially empty. Fill in a brief overview (what this repo is, who it's for) and an "Installation" / "Running tests" section pointing at `make install` and `make test`. Keep it short — this is a personal keyboard config, not a public library.

Voice: simple plain language, no em dashes (matches existing CLAUDE.md style).

## User stories covered

- 29 — README points newcomers at `make install` and `make test`
- 30 — CLAUDE.md reflects that LuaRocks and busted are part of the toolchain

## Acceptance criteria

- [ ] CLAUDE.md no longer lists "no package manager beyond Homebrew" as a deliberate non-choice.
- [ ] CLAUDE.md has a section describing busted, LuaRocks, `lua_modules/`, `make test`, and `spec/` layout.
- [ ] CLAUDE.md's architecture overview mentions `space-fn-engine.lua` and `space-fn.lua` and no longer references `super.lua` / `super-new.lua` / `super-backup.lua`.
- [ ] README.md has an overview and pointers to `make install` and `make test`.
- [ ] Both docs use simple plain language and no em dashes.

## Blocked by

- [03. wire space-fn adapter and delete scratchpads.md](<./03. wire space-fn adapter and delete scratchpads.md>)
